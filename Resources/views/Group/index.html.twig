{% extends "::base.html.twig" %}

{% block innerbody %}
    <h2>Groups</h2>

    <table class="table table-striped">
    <thead>
        <tr>
            <th>Name</th>
            <th>Role</th>
            <th>Active</th>
            <th>Date Modified</th>
            <th>Date Created</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('orkestra_group_show', { 'id': entity.id }) }}">{{ entity.name }}</a></td>
            <td>{{ entity.role }}</td>
            <td>{{ entity.active }}</td>
            <td>{% if entity.dateModified %}{{ entity.dateModified }}{% endif %}</td>
            <td>{% if entity.dateCreated %}{{ entity.dateCreated }}{% endif %}</td>
            <td>
                <a class="btn btn-mini" href="{{ path('orkestra_group_edit', { 'id': entity.id }) }}">EDIT</a>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>
<a class="btn" href="{{ path('orkestra_group_new') }}">
    Create a new entry
</a>
{% endblock %}
