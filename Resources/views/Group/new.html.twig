{% extends '::base.html.twig' %}

{% block body %}
<form action="{{ path('orkestra_group_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p class="form-buttons">
        <button type="submit">Create</button>
    </p>
</form>

{% endblock %}
