{% extends '::base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block content %}
{{ form_start(form, {'action':  path('orkestra_user_update', { 'id': user.id }) , 'method': 'post' }) }}
<div class="well">
    {{ form_widget(form) }}
    <p class="form-buttons">
        <button type="submit" class="btn btn-primary">Save Changes</button>
    </p>
</div>
{{ form_end(form) }}
{% endblock %}
