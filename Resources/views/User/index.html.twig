{% extends "::base.html.twig" %}

{% block body %}
    <h1>User list</h1>

    <table class="records_list">
    <thead>
        <tr>
            <th>Firstname</th>
            <th>Lastname</th>
            <th>Username</th>
            <th>Active</th>
            <th>Datemodified</th>
            <th>Datecreated</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('orkestra_user_show', { 'id': entity.id }) }}">{{ entity.firstName }}</a></td>
            <td>{{ entity.lastName }}</td>
            <td>{{ entity.username }}</td>
            <td>{{ entity.active }}</td>
            <td>{% if entity.dateModified %}{{ entity.dateModified|date('Y-m-d H:i:s') }}{% endif %}</td>
            <td>{% if entity.dateCreated %}{{ entity.dateCreated|date('Y-m-d H:i:s') }}{% endif %}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('orkestra_user_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('orkestra_user_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

    <ul>
    <li>
        <a href="{{ path('orkestra_user_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
{% endblock %}
