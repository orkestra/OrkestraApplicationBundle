{% extends '::base.html.twig' %}

{% block title %}My Profile{% endblock %}

{% block content %}
    <form action="{{ path('orkestra_profile') }}" method="post" {{ form_enctype(form) }}>
<div class="well">
    {{ form_widget(form) }}
    <p class="form-buttons">
        <button type="submit" class="btn btn-primary">Save Changes</button>
        <a href="{{ path('orkestra_profile_password') }}" class="btn">Change Password</a>
    </p>
</div>
</form>
{% endblock %}
